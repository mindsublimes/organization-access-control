<h1>Manage Memberships for <%= @group.name %></h1>

<%= form_with url: group_group_memberships_path(@group), method: :post, local: true do |f| %>
  <div class="field">
    <%= label_tag :email, 'Add member by email' %>
    <%= text_field_tag :email %>
    <%= f.submit 'Add Member' %>
  </div>
<% end %>

<table>
  <thead>
    <tr>
      <th>Email</th>
      <th>Role</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @group_memberships.each do |membership| %>
      <tr>
        <td><%= membership.user.email %></td>
        <td>
          <%= form_with url: group_group_membership_path(@group, membership), method: :patch, local: true do |f| %>
            <%= select_tag :role, options_for_select(GroupMembership.roles.keys, membership.role) %>
            <%= f.submit 'Update Role' %>
          <% end %>
        </td>
        <td>
          <%= button_to 'Remove', group_group_membership_path(@group, membership), method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Back to Group', group_path(@group) %>
